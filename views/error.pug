doctype html
html(lang="en")
  include common/head.pug
  +head("Error: " + error.status + "erkkimadisson.net")
  body
    h1= message
    h2= error.status
    pre #{error.stack}
    
    include common/footer.pug
    +footer
